# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Vite / TS cache
.vite/
*.tsbuildinfo

# stray debug screenshots dropped in the project root
/*.png

# Packaged builds staged for handing out (exe / installer / dmg)
/releases/

# Cross-machine handoff folders (build outputs + patches, merged by hand)
/PlanMapper-mac-handoff/

# macOS AppleDouble sidecars, created when a Mac writes to a FAT/exFAT volume
._*

# Reference screenshots of client work — kept locally, never published
PDF-JPG samples/

# Local-only working files. Kept on the dev machine, deliberately out of the
# repo: icon source art (the generated icons the build uses live in
# planscale/src-tauri/icons/), Soundvision round-trip test files, the legacy
# dev-server launcher, and the note that ships beside the Mac download.
planmapper-icons/
SV import example.txt
sv export text.txt
Start PlanMapper.bat
MAC_USERS_READ_ME_FIRST.txt
planscale/README.md
